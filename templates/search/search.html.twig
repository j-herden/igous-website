{% extends "base.html.twig" %}

{% block content %}
    <h2>Zeige {{ hits|length }} Treffer für "{{ query }}"</h2>

    <ul>
        {% for i, hit in hits %}
            <li>
                <a href="{{ hit.document.url }}">
                    {{- hit.document.title -}}
                </a>
            </li>
        {% endfor %}
    </ul>
{% endblock %}
